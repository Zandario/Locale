name: Run CI

on:
  push:
    # branches:
    #   - "main"
    paths-ignore:
      - '**/*.md'
  pull_request:
    branches:
      - "main"
    paths-ignore:
      - '**/*.md'
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Validate Locale
      uses: Zandario/locale-checker-action@bcb4f8a75152ca21db7002d219f17810068163f3
