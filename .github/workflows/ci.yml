name: Run CI

on:
  push:
    # branches:
    #   - "main"
    paths-ignore:
      - '**/*.md'
  pull_request:
    branches:
      - "main"
    paths-ignore:
      - '**/*.md'
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Validate Locale
      uses: Zandario/locale-checker-action@ee648cc94b5bd18ca7f59c74f0aeb1eec9d58bdb
