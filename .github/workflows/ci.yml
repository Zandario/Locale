name: Run CI

on:
  push:
    # branches:
    #   - "main"
    paths-ignore:
      - '**/*.md'
  pull_request:
    branches:
      - "main"
    paths-ignore:
      - '**/*.md'
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Validate Locale
      uses: Zandario/locale-checker-action@3b28871b9d91852004dc8a4bb2866760f022295d
